import { StyleSheet, Text, View } from "react-native";
import React from "react";

const FormulaScreen = () => {
  return (
    <View>
      <Text>Норм дүрэм</Text>
    </View>
  );
};

export default FormulaScreen;

const styles = StyleSheet.create({});
